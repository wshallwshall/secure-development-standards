# Sidebar navigation, rendered by _layouts/default.html.
#
# `path` is site-root-relative and does NOT carry the baseurl -- the layout prepends
# {{ site.baseurl }}. It is also what page.url is compared against to mark the current page, so the
# two have to stay in the same form: a docs/FOO.md page is built to /FOO.html, because no permalink
# style is configured in _config.yml. If a permalink style is ever added, every path here changes.
#
# `label` is the short nav name and follows the filename. `title` is the document's own H1 verbatim,
# which is not always the same thing -- ASVS-ASSESSMENT.md never says ASVS in its heading -- and is
# used as the link's title attribute.
#
# An entry with `url` instead of `path` is an absolute link off the site.
#
# A page in a SUBDIRECTORY follows the same rule: docs/standards/FOO.md is built to
# /standards/FOO.html. ONE FILENAME BREAKS THAT RULE, which is why the section index is called
# OVERVIEW.md and not README.md. jekyll-optional-front-matter refuses to convert a front-matter-less
# file whose basename is on its FILENAME_BLACKLIST (README, LICENSE, CONTRIBUTING and friends), so a
# README stays a static file; jekyll-readme-index then republishes it as the DIRECTORY INDEX with
# permalink File.dirname(url) + "/", i.e. /standards/, and the raw .md is served as well. There is no
# /standards/README.html for a nav path to point at. Both plugins are in the GitHub Pages
# always-enabled set and cannot be turned off; the two config keys that would change the behaviour,
# readme_index.with_frontmatter and readme_index.remove_originals, are unset in _config.yml. Naming
# the file OVERVIEW.md keeps it on the ordinary path, so its URL is predictable and page.url matches.
#
# ORDER IS A ROUTING DECISION, not an accident. A reader arrives wanting one of three things: to find
# out whether any of this applies to them, to hand something to an executive, or to adopt a named
# control. "Start here" answers the first two in three rows. Within every other group the order is
# alphabetical, because a menu runs on recognition rather than recall and an order nobody can predict
# is worse than one nobody chose.

- group: Start here
  items:
    - label: Start here
      title: Security standards for teams building with AI coding assistants
      path: /
    - label: Which standards apply to you
      title: "Which security standards actually apply to you?"
      path: /standards/WHICH-STANDARDS-APPLY.html
    - label: CISO summary
      title: The CISO summary
      path: /standards/CISO-SUMMARY.html

- group: The standards
  items:
    - label: Standards index
      title: Standards you can start from
      path: /standards/OVERVIEW.html
    - label: AI-assisted development
      title: "AI-assisted development: keeping the result trustworthy"
      path: /standards/AI-ASSISTED-DEVELOPMENT.html
    - label: Automated compliance in CI
      title: "Automated compliance in CI: which of your rules can actually stop a change"
      path: /standards/CI-ENFORCEMENT.html
    - label: Code quality
      title: Judging code quality, whoever or whatever wrote it
      path: /standards/CODE-QUALITY.html
    - label: Dependency integrity
      title: Dependency and artifact integrity
      path: /standards/DEPENDENCY-INTEGRITY.html
    - label: Human review of code
      title: How much of AI-generated code does a human have to read?
      path: /standards/REVIEW-DEPTH.html
    - label: Secure development
      title: "Secure development: a starting point for your own standard"
      path: /standards/SECURE-DEVELOPMENT.html

- group: Adopting and proving it
  items:
    - label: How to adopt these
      title: "How to adopt these"
      path: /standards/ADOPTING-THESE.html
    - label: Standards reference
      title: "Security standards reference: what each one issues, and what triggers it"
      path: /standards/STANDARDS-REFERENCE.html
    - label: What a buyer will ask you for
      title: "What to have ready when a buyer asks, and what each artifact proves"
      path: /standards/DILIGENCE-PACKET.html

- group: Method
  items:
    - label: CI and standards
      title: CI and standards for agent-written code
      path: /CI-AND-STANDARDS.html
    - label: House style
      title: House style
      path: /HOUSE-STYLE.html
    - label: Use OWASP ASVS 5.0
      title: Running a large security-standard assessment with AI agents
      path: /ASVS-ASSESSMENT.html

- group: On GitHub
  items:
    - label: Source repository
      title: secure-development-standards on GitHub
      url: https://github.com/wshallwshall/secure-development-standards
    # The toolkit these documents were written alongside and which is still their worked
    # demonstration. It is a separate repository since the split, and these two rows are the only
    # deliberate links out to it -- everything else that named it was repointed here.
    - label: The toolkit
      title: claude-multisession on GitHub
      url: https://github.com/wshallwshall/claude-multisession
    - label: CLAUDE.md template
      title: A working agreement to copy into your own repository
      url: https://github.com/wshallwshall/claude-multisession/blob/main/CLAUDE.md.template
